%h1= t('views.menus.menu')

= render partial: 'meals/courses', collection: @facade.courses, locals: { facade: @facade }

- if @facade.menu.created_at.today?
  = render 'meals/form', menu: @facade.menu, meal: @facade.new_meal

= link_to t('views.done'), menu_path(@facade.menu)
