%li= "Order for #{order.stamp}:#{render partial: 'orders/meal', collection: order.meals} | #{link_to 'Edit Order', edit_menu_order_path(order.menu, order)} | #{link_to 'Delete Order', menu_order_path(order.menu, order), method: :delete} "
